dependencies:
  # Selected production transport. Device-B C6/Wi-Fi dependencies were
  # removed; deprecated TCP source remains in-tree but is not built.
  espressif/esp_tinyusb: "1.7.6~1"
  idf: ">=5.5"
