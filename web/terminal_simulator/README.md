# Terminal simulator

This directory is deliberately static HTML/CSS/JavaScript. It imports no
AetherSparse runtime module and communicates only with the accessory API.

Run it as a separate process:

```bash
python -m http.server 8080 --directory web/terminal_simulator
```

